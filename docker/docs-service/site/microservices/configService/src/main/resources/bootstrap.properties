spring.application.name=config-service

eureka.client.serviceUrl.defaultZone=http://discovery:8761/eureka/

server.port=8888

management.security.enabled=false
spring.cloud.config.override-none=true

#spring.cloud.config.server.git.uri=git@bitbucket.org:radiantsolutions/services-config.git

# Use the following configuration options instead of the above to use a local
# directory directly, rather than a git-based configuration repository.
spring.profiles.active=native
spring.cloud.config.server.native.searchLocations=file://blueground/services-config
